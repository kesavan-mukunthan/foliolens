# spec-00 — Return engine, validated on known sample funds

**Status:** ready for build.
**Executor:** Claude Code on Sonnet 4.6. Sub-steps 0.4–0.6 require Sonnet; 0.2 / 0.3 / 0.7 are Haiku-safe.
**Conventions:** see `CLAUDE.md`. Do not restate or override them.
**Architecture:** see `ARCHITECTURE.md` for the domain model. Step 0 builds its priced-instrument subset; everything else is defined-but-stubbed.

## Objective
NAV → returns for a fixed set of known funds, validated three ways (own impl, library oracle, published factsheet figures). This is the keystone calc; everything downstream depends on it. Validate before any personal data, scaling, or cloud.

## In scope
- **Domain model layer** — the step-0 subset of `ARCHITECTURE.md`: `Investment`, `NavSeries`, `ReturnSource` protocol + `PricedSource`, `ShareClass`/`Fund`, engine. Deferred types (`Cashflow`, `BlendSource`, `HeldSource`, `WeightPolicy`, holdings DAG) are **defined as stubs** to keep the contract stable.
- Fetch growth-option NAV history for the reference funds via mftool.
- Land raw daily NAV as `decimal128` parquet behind a single `DataAccess` seam.
- Derive the month-end NAV series.
- `ReturnSource` protocol + return engine (TWR; absolute/CAGR under the SEBI rule).
- Three-way validation harness with the fixed tolerances.
- pytest test layer — the gate. See `tests/TESTS.md`.
- Excel validation report (xlsxwriter), write-only.

## Out of scope — do not build
- Universe-scale ingestion: no daily Cloud Run job, no upsert/staleness alarm, no full-universe fetch. That is step 4.
- GCS / Docker / GitHub Actions. **Local only.**
- Risk metrics (Sharpe/drawdown/vol) — protocol surface only; implemented at step 2.
- CAS parsing, cashflows, XIRR/MWR — step 1+.
- Holdings DAG, look-through exposure, blends, weight curves, MWR — **defined as stubs only**; built in later steps (see `ARCHITECTURE.md`).
- `pyxirr` — not used here.

## Human-supplied inputs — NOT for the executor to source
The executor must not source AMFI codes or scrape factsheets; both are silent-error sources. The builder provides these at task 0.1:
- `fixtures/funds.csv`: `amfi_code, isin, option, plan, fund_name, category` — one row per share class. Cover: large cap ×2, mid cap, small cap, flexi cap, short-duration debt, liquid, fund-of-funds, multi-asset. Include at least one fund as **both** direct-growth and regular-growth (for the direct ≥ regular invariant).
- `fixtures/published_returns.csv`: `amfi_code, period (1Y|3Y|5Y|SI), published_return_pct, factsheet_as_of, source_url` — figures read off the AMC factsheet. `factsheet_as_of` is authoritative; `source_url` is a convenience pointer that may rot.
- `fixtures/nav_snapshots/` — frozen NAV per fund for deterministic tests. **Generated by the freeze script at 0.2b, not by hand** — committed once, then immutable.

If either CSV is absent or incomplete, **stop and report — do not invent values.**

## Sub-steps
Each ≈ one 45-min session.

### 0.0 Scaffold — Haiku-safe
- Repo tree (below); `pyproject.toml` with deps; `CLAUDE.md` present. No Docker/CI.
- **Accept:** `pytest` runs (zero tests ok); package imports cleanly.

### 0.1 Reference set + published figures — builder, manual (not the executor)
- Builder fills **only** `funds.csv` and `published_returns.csv`. NAV snapshots are not hand-made — they are frozen by code at 0.2b.
- Each `factsheet_as_of` must fall within the fund's NAV history range, so the reconciliation endpoint exists once the snapshot is frozen.
- **Accept:** both CSVs validate against schema; every `amfi_code` in published_returns exists in funds.

### 0.2 mftool client — Haiku-safe
- `ingest/mftool_client.py`: fetch full NAV history by `amfi_code`. **Verify mftool's actual response shape first and adapt the normaliser — do not assume the schema.** Isolate all mftool calls here. Normalise to `(amfi_code, date, nav)` with `nav` as `Decimal`. Distinguish holidays/weekends from missing data; **no forward-fill in the raw layer.**
- **Accept:** typed, sorted, de-duplicated series; no `(code, date)` dupes; `nav` is `Decimal`.

### 0.2b Freeze NAV snapshots — Haiku-safe (Claude Code runs once)
- `scripts/freeze_snapshots.py`: read `funds.csv`, fetch each fund's history via the mftool client, write frozen parquet to `fixtures/nav_snapshots/`. Run **once** and **commit the output**; thereafter tests read frozen files and never touch the network.
- Record the freeze date; verify every `factsheet_as_of` ≤ the fund's snapshot last date.
- **Accept:** snapshots present for all funds; each fund's snapshot covers through ≥ its `factsheet_as_of`.

### 0.3 Raw land + DataAccess seam — Haiku-safe
- `ingest/land.py`: write raw NAV to `data/raw/` as parquet, `decimal128`, partitioned by `amfi_code`.
- `data_access.py`: the **single** read path. DuckDB reads parquet via a path that is local now and `gs://` later. No other module reads parquet paths directly.
- **Accept:** `test_decimal_roundtrip` green; spot-check 3 dates against AMFI published NAV.

### 0.4 Month-end base — Sonnet
- Month-end logic lives on `NavSeries.month_end()` (`model/value_objects.py`): last NAV on/before calendar month-end; no look-ahead; incomplete leading/trailing months skipped, not fabricated. Daily retained.
- **Accept:** `test_weekend_boundary` and `test_no_lookahead` green.

### 0.5 Domain model + engine — Sonnet
- `model/` (per `ARCHITECTURE.md`): `Investment`, value objects (`NavSeries`, `ReturnSeries`, `ReturnResult`, `Cashflow`), `ReturnSource` protocol + `PricedSource`; `ShareClass`/`Fund` concrete (Fund prices via a representative ShareClass). Stub `BlendSource`, `HeldSource`, `WeightPolicy`, holdings/`Cash` — defined, no logic. Risk-metric methods declared, not implemented.
- `returns/engine.py`: `period_return(source, period, as_of)` — TWR; absolute (<1Y) / CAGR (≥1Y) under the SEBI rule; `years = actual_days/365`; `Decimal` throughout, float only at the library boundary. One function, every source type.
- **Accept:** invariant tests green — SEBI both directions, chaining-consistency, TWR == point-to-point on a cashflow-free series; `ShareClass` satisfies `ReturnSource` (mypy).

### 0.6 Three-way validation — Sonnet
- `validation/oracle.py`: wrap ffn (primary) and empyrical-reloaded (optional); **declare periodicity explicitly**.
- `validation/reconcile.py`: compare own vs oracle vs published; tolerances are the CLAUDE.md constants; out-of-tolerance **fails loud** with fund/period/delta. **Do not loosen tolerances to pass** — report the discrepancy as a finding.
- **Accept:** reconciliation tests green on fixtures; own↔oracle ≤ 1e-6; own/oracle↔published ≤ 10 bps after as-of match.

### 0.7 Runner + Excel report — Haiku-safe
- `report/excel.py` (xlsxwriter, write-only) + `cli.py` runs end-to-end on the reference set.
- Tabs:
  - **Reconciliation** — fund × period × own / oracle / published / delta / pass-fail; failures conditionally highlighted.
  - **Per-fund NAV** — date, daily nav, month-end flag.
  - **Metadata** — per fund: `amfi_code, isin, option, plan`, AMFI scheme page, factsheet `source_url` (hyperlink via `write_url`), `factsheet_as_of`, figure source; plus run as-of, day-count, tolerances.
- Display: NAV 4dp; returns as **%** 2dp; no scientific notation.
- **Accept:** `outputs/validation_report.xlsx` generated; re-run identical; reporter **computes nothing** (reads engine + reconcile outputs only).

## Acceptance — the gate
1. `pytest` all green — invariants + step-0 reconciliation.
2. `python -m foliolens.cli` produces `outputs/validation_report.xlsx`; all funds reconcile within tolerance, or fail loud with an itemised delta.
3. Re-running produces an identical report (determinism).
4. No mftool call outside `mftool_client.py`; no parquet read outside `data_access.py`; no float in any stored/returned value.

## Executor guards — because the executor is mid-tier
- **mftool:** verify the real response shape before coding the normaliser; isolate behind the client.
- **Decimal:** the round-trip test is mandatory and must pass before 0.5.
- **Tolerances** are constants — never adjust them to make a test pass. A failing reconciliation is a finding, not a test to relax.
- Do not source fund codes or factsheet figures; do not read the Excel back; do not call mftool at compute time.

## Dependencies
`mftool, duckdb, pyarrow, pandas, ffn, empyrical-reloaded, pytest, xlsxwriter`. (Not `pyxirr`. `hypothesis` optional.)

## Repo layout
```
foliolens/
  CLAUDE.md
  SCOPE.md
  ARCHITECTURE.md
  pyproject.toml
  specs/step-00-return-engine.md
  src/foliolens/
    data_access.py            # single read seam: local parquet now, gs:// later
    ingest/
      mftool_client.py        # all mftool calls isolated here
      land.py                 # raw NAV -> decimal128 parquet
    model/
      investments.py          # Investment protocol + ShareClass, Fund (concrete); Stock/Portfolio/Benchmark/Cash (stubs)
      value_objects.py        # NavSeries (+ month_end), ReturnSeries, ReturnResult, Cashflow
      sources.py              # ReturnSource protocol; PricedSource; HeldSource/BlendSource (stubs)
      weights.py              # WeightPolicy: Fixed/Drift/PIT (stubs)
      holdings.py             # Holding edge + DAG resolve (stub)
    returns/
      engine.py               # period_return: TWR, absolute/CAGR, SEBI rule
    validation/
      oracle.py               # ffn / empyrical-reloaded, periodicity declared
      reconcile.py            # three-way compare, fixed tolerances, fail loud
    report/excel.py           # xlsxwriter, write-only
    cli.py                    # end-to-end runner -> workbook
  scripts/freeze_snapshots.py # one-time: funds.csv -> fixtures/nav_snapshots/ (run once, commit)
  data/raw/                   # parquet, decimal128, partitioned by amfi_code (gitignored, regenerated)
  fixtures/
    funds.csv                 # human-supplied (0.1)
    published_returns.csv     # human-supplied (0.1)
    nav_snapshots/            # frozen NAV per fund (0.1)
  tests/
    TESTS.md
    conftest.py
    invariants/
    step00/
  outputs/validation_report.xlsx
```
