"""Oracle wrapper for library-based return verification.

Wraps ffn (primary) and empyrical-reloaded (optional) as reference
implementations. Periodicity (daily vs monthly) is declared explicitly to
avoid silent mis-annualisation.
"""
